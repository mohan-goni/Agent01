# ==============================================================================
# Next.js Application Environment Variables (.env.local)
# ==============================================================================

# PostgreSQL database connection string.
# Used by:
# 1. Drizzle ORM (for schema migrations and Next.js application data access).
# 2. Prisma ORM (for `better-auth` client generation and its runtime authentication operations).
# Example: postgresql://user:password@host:port/dbname?sslmode=require
DATABASE_URL="postgresql://user:password@host:port/dbname?sslmode=require"

# Google OAuth Credentials (used by better-auth in Next.js app)
# Obtain these from Google Cloud Console: https://console.cloud.google.com/apis/credentials
GOOGLE_CLIENT_ID="YOUR_GOOGLE_CLIENT_ID"
GOOGLE_CLIENT_SECRET="YOUR_GOOGLE_CLIENT_SECRET"
# This should match the "Authorized redirect URIs" configured in your Google OAuth client settings
# Typically, for local development, it's http://localhost:3000/api/auth/callback/google
GOOGLE_REDIRECT_URI="http://localhost:3000/api/auth/callback/google"

# Better Auth Configuration (for Next.js app)
# A secret key for session encryption. Generate a strong random string.
# Example generation: openssl rand -hex 32
AUTH_SECRET="YOUR_STRONG_RANDOM_SECRET_FOR_AUTH"
# The base URL of your Next.js application
AUTH_URL="http://localhost:3000"

# Python Agent API Communication (for Next.js app)
# Base URL for the Python FastAPI agent, used by Next.js Server Actions to call the Python API.
# - For Vercel deployment: Leave blank or set to "/api/python_agent" (routing via vercel.json).
# - For local dev (Next.js on 3000, Python on 8008 separately): "http://localhost:8008"
# - If using `vercel dev`: Leave blank (it handles the proxy via vercel.json).
# The Next.js actions default to "/api/python_agent" if this is empty, but use "http://localhost:8008"
# during local development if this variable is empty AND NODE_ENV is 'development'.
PYTHON_AGENT_API_BASE_URL=""


# ==============================================================================
# Python Agent API Service Environment Variables (api_python/.env)
# ==============================================================================
# The following variables are NOT used by the Next.js application directly.
# They are required by the Python agent service in the `api_python/` directory.
# You should create an `api_python/.env` file (or set these in Vercel's environment variables for the Python function).
# An example file `api_python/.env.example` is provided.

# Example placeholder for TAVILY_API_KEY (used by agent_logic.py)
# TAVILY_API_KEY="YOUR_TAVILY_API_KEY_FOR_PYTHON_AGENT"

# Example placeholder for GOOGLE_API_KEY (used by agent_logic.py for Gemini)
# GOOGLE_API_KEY="YOUR_GOOGLE_API_KEY_FOR_GEMINI_IN_PYTHON_AGENT"

# Example placeholder for OPENAI_API_KEY (if agent_logic.py is modified to use OpenAI)
# OPENAI_API_KEY="YOUR_OPENAI_API_KEY_FOR_PYTHON_AGENT"


# ==============================================================================
# Optional: Other API keys if Next.js services were to call external APIs directly
# ==============================================================================
# NEWS_API_KEY="YOUR_NEWS_API_KEY_FOR_NEXTJS"
# ALPHA_VANTAGE_API_KEY="YOUR_ALPHA_VANTAGE_KEY_FOR_NEXTJS"
