# Environment variables for the Python Agent API service (api_python/)
# Copy this file to .env in this directory (api_python/.env) and fill in your actual keys for local development.
# For Vercel deployment, set these environment variables directly in the Vercel project settings for the Python function.

# Required by agent_logic.py for Tavily search tool
TAVILY_API_KEY="YOUR_TAVILY_API_KEY_HERE"

# Required by agent_logic.py for Google Gemini LLM (via langchain.chat_models.init_chat_model)
GOOGLE_API_KEY="YOUR_GOOGLE_API_KEY_FOR_GEMINI_HERE"

# Optional: If you modify agent_logic.py to use OpenAI models, you would need this
# OPENAI_API_KEY="YOUR_OPENAI_API_KEY_IF_USING_OPENAI_MODELS"

# Optional: User Agent for web requests (agent_logic.py sets a default if not provided)
# USER_AGENT="CustomUserAgent/1.0"
